<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- MainActivity 的 <activity> 条目由 C# [Activity] 特性自动生成（含 CRC 包名的 Java stub），
       手写相对名条目会指向不存在的类导致 ClassNotFoundException，此处勿手写。 -->
  <application android:label="MediaOrganizer" android:icon="@drawable/icon" android:allowBackup="true" />

  <!-- SAF 目录/文件访问走 DocumentProvider，无需存储权限；网络输出目标与 WakeLock 需要以下权限 -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>
